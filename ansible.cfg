[defaults]
# Use /tmp for temporary files to avoid permission issues when running locally with become
remote_tmp = /tmp
# Disable host key checking to avoid interactive prompts for new hosts
host_key_checking = False
# Default inventory path
inventory = inventory/hosts.ini
